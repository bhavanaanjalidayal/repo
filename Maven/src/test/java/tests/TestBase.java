package tests;

public class TestBase {
	
/* for loading the configurations from properties files, 
 * Initializing the WebDriver, Implicit Waits,
 *  Extent Reports and 
 *  also to create the object of FileInputStream which
 *   is responsible for pointing towards the file from which the data should be read.	
 */

}
